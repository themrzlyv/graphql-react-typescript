import React from 'react';
import {Grid} from "@mui/material";
import MainTable from "../blocks/MainTable/MainTable";

const Body = () => {
    return (
        <Grid item xs={12}>
            <MainTable/>
        </Grid>
    );
};

export default Body;